const initState = {};

const reducer = (state = initState, action) => {
  // console.log("Redux action: ", action.type);
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
